// import { screen, render } from '@testing-library/react';
// import BtnFindUnit from '../components/Form/BtnFindUnit';

describe('Suite de testes com o componente BtnFindUnit', () => {
  // o botão só é renderizado após mudar o estado global de gyms, pois retorna null
  test.todo('Encontre o botão "ENCONTRAR UNIDADE" no documento');
});
